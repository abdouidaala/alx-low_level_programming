#include "main.h"

/**
 * _abs - COmputes the absolute value of an integer
 *
 *@r: The integer to be computed
 *
 * Return: Absolute value
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (r);
	}
	else
	{
		return (r);
	}
}
