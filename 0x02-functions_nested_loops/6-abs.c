#include "main.h"

/**
 * @brief _abs - Computes the absolute value of an integer
 *
 * @param n The integer to be computed
 *
 * @return Absolute value
 */
int _abs(int n)
{
	return ((n < 0) ? -n : n);
}
