#include "main.h"

/**
 * print_last_digit - Prints the last digit of a number
 *
 * @r: A number
 *
 * Return: Last digit of n
 */
int print_last_digit(int r)
{
	int l-digit;

	if (r < 0)
	{
		l-digit = (r % 10) * -1;
		return (last-d);
	}
	else
	{
		l-digit = r % 10;
		return (l-digit);
	}
}
